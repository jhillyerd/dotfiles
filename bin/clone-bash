#!/bin/sh

[[ "$TRACE" ]] && set -x
set -eo pipefail

target_host="$1"

scp_dotfile() {
  declare file="$1"
}

echo "Cloning to $target_host:"

for file in bash_profile bashrc; do
  scp "$HOME/.dotfiles/$file" "$target_host:.$file"
done
